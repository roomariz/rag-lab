# Docker

Docker packages applications and their dependencies into portable containers.
It is useful for consistent builds, local development, and repeatable deployments.

Typical Docker workflows include building images, running containers, and pushing
those images to a registry.
