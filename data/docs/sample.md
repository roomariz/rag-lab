# Sample Corpus

Docker is a platform for packaging applications into lightweight containers.

Kubernetes orchestrates containers and automates deployment at scale.

SQL injection is a vulnerability where attackers manipulate SQL queries.
