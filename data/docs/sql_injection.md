# SQL Injection

SQL injection is a security vulnerability that happens when untrusted input is
concatenated into SQL queries.

It can expose data, modify records, or bypass authentication if queries are not
properly parameterized.
